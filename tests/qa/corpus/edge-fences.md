# Recinti di codice

Con linguaggio:

```rust
fn main() {}
```

Con linguaggio e metadati:

```js title="esempio.js" showLineNumbers
const x = 1;
```

Con tilde:

~~~sql
select 1;
~~~

Con piu' di tre segni:

`````text
qui dentro ci sono ``` tre apici
`````

Senza linguaggio:

```
testo semplice
```

Dentro una voce di elenco:

- una voce

  ```yaml
  chiave: valore
  ```

- un'altra voce
